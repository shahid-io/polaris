# Generated by Next.js on every dev run and build, and its own header says it should not be
# edited. Formatting it is futile: Next rewrites it with double quotes and the check fails
# again on the next build. Ignored rather than reformatted so `pnpm format:check` passes on
# a clean checkout.
apps/web/next-env.d.ts

# Build output and dependencies.
**/.next/
**/dist/
**/node_modules/
